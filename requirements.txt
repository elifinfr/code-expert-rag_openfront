# Code Expert RAG — dependances Python (version TS/JS)
# Python >= 3.12 recommande.

# --- Frameworks agent ---
langchain>=1.0.0
langgraph>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0
langchain-text-splitters>=1.0.0

# --- Connecteurs bases ---
langchain-neo4j>=0.6.0
neo4j>=5.0.0
qdrant-client>=1.15.0
langchain-qdrant>=1.0.0
langchain-chroma>=1.0.0
chromadb>=0.5.0

# --- Chunker (tree-sitter) ---
tree-sitter>=0.25.0
tree-sitter-language-pack>=0.10.0

# --- Loader SCIP (parsing index protobuf) ---
protobuf>=4.0.0
grpcio-tools>=1.60.0

# --- Divers ---
pydantic>=2.0.0
